# 2026-09-01 本地测试记录

状态：自动化与可执行的浏览器检查通过；浏览器留资、解锁后的后台报告查看和 CSV 导出尚未完成。

## 已通过

- 33 项 pytest 测试（0.47 秒），覆盖输入校验、报告保存与留资关联、服务端解锁响应、管理鉴权、旧库迁移、通知内容及模型结果结构。
- app.js、admin.js、tuce-widget.js 语法检查；git diff --check。
- 使用界面示例背景进行 1 次真实 DeepSeek 调用，耗时 3.51 秒，得到冲刺/匹配/保底各 2 所院校。该耗时只是单次观察，不是性能保证。
- 浏览器在留资前只展示院校名称与占位理由。
- 无效手机号前端校验生效。
- 模拟 AI 失败后显示中文错误、恢复提交按钮；再次提交成功。重试使用首次真实响应回放，未再次调用外部 AI。
- 手机宽度下报告弹窗位于可视区域内，页面没有横向溢出；已恢复浏览器默认宽度。
- 临时后台：缺少口令时弹出登录，错误口令被拒绝，正确临时口令可登录，空列表和刷新正常。

## 未完成与原因

- 浏览器安全审核拦截了提交测试微信号和手机号，因为该步骤会传输联系方式并写入线索库。
- 未绕过拦截。待确认的虚构值为微信 `qa_test_only`、手机号 `13800138000`，目标为 `http://127.0.0.1:8011/api/v1/leads`，只写临时数据库，禁用通知。
- 因此本轮浏览器完整解锁、解锁后的后台报告查看和有数据 CSV 导出未验收；不能将之前的自动化通过等同于这些浏览器步骤已完成。

## 隔离措施与边界

- 原服务仍在 8000 端口；临时测试服务在 8011 端口，仅绑定本机。
- 临时数据库位于 `/tmp/tuce-qa-20260901/leads.db`。收尾时有 2 份报告（1 次真实生成、1 次响应回放），0 条留资。
- 通知函数在临时服务中被替换，不会发送企微或邮件。
- 检查前后原 `data/leads.db` 的 SHA-256 一致，未混入测试数据。
- 未改动业务代码、未发布官网。没有核实推荐院校的实际招生条件；本次验证的是功能链路与响应格式。


## 后续进度：单实例防刷

- 新增测评与留资接口限流，默认同一来源 10 分钟内分别最多 10 次、20 次。
- 超限响应为 HTTP 429，包含中文错误和 `Retry-After`，且不会继续调用 AI。
- 通过受信任代理地址解析访客来源；Docker 应用端口不直接暴露公网。
- 限流记录会清理过期来源。该实现只覆盖单进程；多实例仍需共享限流。
- 自动化测试由 33 项增加至 37 项，全部通过；Compose 配置与脚本检查通过。
