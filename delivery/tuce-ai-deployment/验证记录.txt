途策 AI 智能选校部署材料验证记录
日期：2026-09-03

1. 自动化测试：39 项通过（使用隔离数据库，不调用真实 AI、不发送通知）。
2. 两套 Docker Compose 配置：均通过 config --quiet 校验，使用占位值，未读取真实配置。
3. 前端 app.js、admin.js、tuce-widget.js：语法检查通过。
4. git diff --check：通过。
5. 压缩包采用明确文件清单打包，并校验内部文件哈希；不含真实 .env、数据库、历史留资、备份或虚拟环境。

未完成：目标服务器镜像构建、容器运行、HTTPS 证书申请、真实 AI 和通知链路、备份恢复演练、官网按钮联调。
本机 Docker 引擎探测受权限限制，未做容器构建测试。
